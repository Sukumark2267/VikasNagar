# Campaign image placeholders

Replace the SVG placeholders in this folder with verified, permission-cleared photographs of Kuldeep Kumar and public activities. Keep the same filenames to update the site without code changes, or update `src/data/galleryData.js` when adding new files.
